apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: forgewind-ingress
  namespace: forgewind
  labels:
    app.kubernetes.io/part-of: forgewind
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.forgeengine.dev
      secretName: forgewind-tls
  rules:
    - host: api.forgeengine.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 4000
