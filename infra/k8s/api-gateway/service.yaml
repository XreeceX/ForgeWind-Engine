apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: career-os
  labels:
    app: api-gateway
    app.kubernetes.io/part-of: career-os
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
