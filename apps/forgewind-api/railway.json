{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && HUSKY=0 pnpm install --frozen-lockfile && pnpm --filter @forgewind-engine/config build && pnpm --filter @forgewind-engine/utils build && pnpm --filter @forgewind-engine/forgewind-api build",
    "watchPatterns": [
      "apps/forgewind-api/**",
      "packages/config/**",
      "packages/utils/**",
      "pnpm-lock.yaml",
      "package.json",
      "pnpm-workspace.yaml"
    ]
  },
  "deploy": {
    "startCommand": "node apps/forgewind-api/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
