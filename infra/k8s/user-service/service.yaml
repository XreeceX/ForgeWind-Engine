apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: forgewind
  labels:
    app: user-service
    app.kubernetes.io/part-of: forgewind
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 4001
      targetPort: 4001
      protocol: TCP
      name: http
